
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all logos
    // Allow authenticated users to write to their own logo directory
    match /logos/{userId}/{fileName} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
